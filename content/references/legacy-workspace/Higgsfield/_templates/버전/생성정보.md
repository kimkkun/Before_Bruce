# {{SHOT_ID}} {{VERSION}}

- 생성일·생성 서비스/기능 모드/모델(확인 가능한 버전):
- 작업 종류: 최초 생성 / 사용자 피드백 반영 / 독립 대안 / 후처리
- 원본·참조 파일·캐릭터 ID(원래 위치 연결):
- 기반 버전·반영할 피드백 ID: 최초 생성이면 해당 없음
- 이번에 바꿀 요소·유지할 요소:
- 설정: 길이 / 화면비 / 해상도 / 프레임률 / 시드 등 실제 제공된 값만
- 입력 이미지/영상의 버전·파일 해시 또는 서비스 ID(앞 단계 연결):
- 생성 작업 ID·결과 URL:
- 예상 크레딧·사용 승인 근거·확인된 실제 차감: 모르면 미확인

## 실제 전송한 프롬프트

```text
여기에 실제 사용한 프롬프트를 보존한다.
```

## 결과·검토

- 다운로드한 원출력 파일: 미생성
- 편집용 변환 파일·원출력과 달라진 설정: 없으면 해당 없음
- 편집에 사용 가능한 구간·음향 처리 조건:
- 확인한 프레임·영상 구간·오디오:
- 통과한 점·남은 결함·다음 시도:

모델이 공개하지 않은 설정은 추정해 채우지 않는다. 이 버전의 결과와 생성 조건을 덮어쓰지 않고, 수정·재생성은 다음 번호에 저장한다. 현재 채택·승인 상태는 상위 `제작기록.md`에서만 관리한다.
